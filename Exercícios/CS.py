import numpy as np
from os import chdir, getcwd
print('O diretório atual é: ', chdir('dados'), getcwd())
#fn = 'document.txt'
#dt = np.loadtxt(fn, delimiter='\t', skiprows=1, usecols=[0,2])
#print(dt)